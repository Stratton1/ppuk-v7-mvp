# MOCKS GUIDE (Deprecated)

The mock Supabase layer has been removed. The frontend now always uses the real Supabase browser client with `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`. `/dev/mock-users` is disabled and no mock client switching is supported.
