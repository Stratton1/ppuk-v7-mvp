/**
 * Placeholder: Property flags subsystem is disabled in v7 (no property_flags table).
 * Returns null to avoid build-time schema mismatches.
 */

export const PropertyFlags = (_props: { propertyId: string }) => null;
