# src/integrations

Clients and adapters for external systems/APIs (e.g., gov data sources, edge-function proxies). Enforce validation, retry/backoff, and do not expose secrets; always proxy through secure backends.
