# src/app

Application composition layer: wires the HTTP server/app instance, registers routes and middleware, and provides the entry point glue. Keep this folder for assembly only; business logic belongs in controllers/services.
