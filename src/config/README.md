# src/config

Environment configuration and app-level settings (e.g., env parsing, feature flags, third-party client setup). Keep secrets out of code; use environment variables and validate them before use.
